# Auth service runtime deps (mirrors backend/requirements-base.txt)
Flask==3.1.0
SQLAlchemy==2.0.36
PyJWT==2.10.1
bcrypt==4.2.1
pydantic[email]>=2.13.4,<3
python-dotenv==1.0.1
gunicorn==23.0.0
flask-cors==5.0.0
# Postgres driver (Supabase) — SQLite needs no driver for local dev
psycopg[binary]>=3.2,<4
