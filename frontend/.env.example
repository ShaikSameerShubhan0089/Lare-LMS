# Frontend dev config
# Dev server proxies /api → this target. Now that the API Gateway exists, point
# at it (:8000); the Gateway routes onward to Auth (:8001) and other services.
VITE_API_TARGET=http://127.0.0.1:8000
