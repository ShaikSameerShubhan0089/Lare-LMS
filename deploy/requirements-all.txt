# Consolidated runtime deps for the single production backend image.
# Superset of backend/requirements-base.txt + every service's extras, so one
# image can run all 27 processes (gateway + 26 services) exactly like run-all.ps1.
Flask==3.1.0
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]>=3.2.10
PyJWT==2.10.1
cryptography>=44.0.0
bcrypt==4.2.1
pydantic[email]==2.10.4
python-dotenv==1.0.1
flask-cors==5.0.0
gunicorn==23.0.0
gevent==24.11.1
waitress==3.0.2
requests==2.32.3
redis==5.2.1
boto3==1.35.0
# Optional — enables LIVE Claude in ai_orchestration (STUB mode without it).
anthropic>=0.40.0
